/**
 * @file tadpoleos_service.ts (Legacy Bridge)
 * @description Bridges legacy components to the centralized TadpoleOS service.
 */
import { tadpole_os_service as root_service } from '../../../src/services/tadpoleos_service';

export const tadpole_os_service = root_service;
export default root_service;
