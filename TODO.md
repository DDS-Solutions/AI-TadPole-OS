# Tadpole OS Technical Debt & Backlog

## Maintenance
- [x] Prune unused imports across the `server-rs` project (identified as warnings in `cargo check`).
- [x] Consolidate duplicated logic in `agent/runner/synthesis.rs` and `mod.rs`.
